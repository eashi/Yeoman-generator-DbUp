{
  "name": "generator-dbup",
  "version": "1.0.0",
  "description": "C# Console project to run DbUp migration scripts.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "DbUp Team",
  "license": "MIT",
  "dependencies": {
    "mkdirp": "^0.5.1",
    "yeoman-generator": "^2.0.0"
  }
}
